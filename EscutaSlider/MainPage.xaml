<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:EscutaSlider" x:Class="EscutaSlider.MainPage">
    <StackLayout>
        <!-- Place new controls here -->
        <Label Text="-- ELEMENTO --" 
               HorizontalOptions="Center" 
               VerticalOptions="CenterAndExpand"
               BindingContext="{x:Reference Name=slider_toperson}"
               Rotation="{Binding Path=Value}"
               />
        
        <Slider x:Name="slider_toperson"
                Minimum="0"
                Maximum="360" />
    </StackLayout>
</ContentPage>